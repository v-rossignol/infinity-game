{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://infinity/contracts/schemas/shared/planet-location.json",
  "title": "PlanetLocation",
  "description": "Planet identity with optional hex surface coordinates, in-hex position, and building zone slot (building units).",
  "x-infinity-source": "infinity/src/shared/interfaces/player-location.interface.ts#PlanetLocation",
  "type": "object",
  "properties": {
    "id": { "type": "string", "format": "uuid" },
    "hex_coords": { "$ref": "hex-coords.json" },
    "position": { "$ref": "vec2-local.json" },
    "buildingZoneId": {
      "type": "string",
      "description": "Authoritative building zone slot for immobile building units (e.g. central-1-1, right). Set when the building is spawned; pairs with normalized position at the zone center."
    }
  },
  "required": ["id"],
  "additionalProperties": false
}
